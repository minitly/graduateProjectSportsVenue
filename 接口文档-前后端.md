## 体育馆预约与仓库管理系统 - 前后端接口文档

本文档面向前后端联调使用，**只保留关键接口信息**，所有鉴权细节、表结构等请参考 `项目文档后端说明.md` 与 `table.sql`。

---

## 一、通用约定

- **服务基础地址**：`http://localhost:9999`
- **应用上下文路径**：`/sportsVenue`
- **完整接口前缀**：`http://localhost:9999/sportsVenue`
- **数据格式**：`application/json`
- **字符编码**：UTF-8
- **统一响应结构**：

```json
{
  "code": 200,
  "message": "OK",
  "data": {}
}
```

- **字段说明**：
  - `code`：业务状态码（200 成功，其他失败）
  - `message`：提示信息
  - `data`：业务数据对象/数组；无数据时可为 `null`

### 1.1 认证与 Token 概述

- **认证方式**：用户名 + 密码 + 角色 登录
- **Token 类型**：JWT
- **传递方式**：请求头 `Authorization: Bearer <token>`
- **特点**：后端不保存 Session，**纯 JWT，无状态**，详情见 `项目文档后端说明.md`

受保护接口（需要登录）必须在请求头中携带 Token：

```text
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.xxx.yyy
```

---

## 二、用户注册与登录接口

### 2.1 用户注册接口

#### 2.1.1 接口说明

用于创建系统账号。注册成功后，用户可使用该账号配合角色信息进行登录。

- 普通用户：`USER`（注册**不受限制**）
- 场地主/场地管理者：`OWNER`（注册时必须提供系统 ADMIN 的密码）
- 系统管理员：`ADMIN`（系统唯一账号，由运维/初始化脚本创建，不提供注册入口）

#### 2.1.2 请求信息

- **URL**：`POST /sportsVenue/auth/register`
- **Method**：`POST`
- **Content-Type**：`application/json`

#### 2.1.3 请求参数

| 参数名         | 类型     | 是否必填 | 说明                                                         |
| ------------ | ------ | ---- |------------------------------------------------------------|
| username     | String | 是    | 用户名，需唯一                                                   |
| password     | String | 是    | 登录密码                                                       |
| realName     | String | 是    | 真实姓名                                                       |
| role         | String | 是    | 角色：`USER` 或 `OWNER`（不对外开放注册 `ADMIN`）                  |
| adminPassword| String | 否    | 当 `role = "OWNER"` 时必填，需输入系统唯一 ADMIN 账号的登录密码；当 `role = "USER"` 时前端传空字符串或不传 |
| phone        | String | 否    | 手机号                                                        |
| email        | String | 否    | 邮箱                                                         |

#### 2.1.4 请求示例

```json
{
  "username": "zhangsan",
  "password": "123456",
  "realName": "张三",
  "role": "USER",
  "adminPassword": "",
  "phone": "13800000000",
  "email": "zhangsan@example.com"
}
```

OWNER 注册示例（需填写 ADMIN 密码）：

```json
{
  "username": "owner01",
  "password": "123456",
  "realName": "场馆管理员01",
  "role": "OWNER",
  "adminPassword": "Admin真实登录密码",
  "phone": "13800000001",
  "email": "owner01@example.com"
}
```

#### 2.1.5 成功响应示例

```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1,
    "username": "zhangsan",
    "realName": "张三",
    "role": "USER"
  }
}
```

#### 2.1.6 常见失败响应

- 用户名已存在：

```json
{
  "code": 400,
  "message": "用户名已存在",
  "data": null
}
```

- 角色不允许注册（如尝试注册 ADMIN）：

```json
{
  "code": 400,
  "message": "不支持的注册角色",
  "data": null
}
```

- OWNER 注册时 ADMIN 密码错误：

```json
{
  "code": 403,
  "message": "ADMIN 密码错误，禁止注册场地管理员",
  "data": null
}
```

---

### 2.2 用户登录接口

#### 2.2.1 接口说明

用于完成系统用户登录认证。前端发送用户名、密码及登录身份，后端校验成功后返回用户信息、访问令牌（JWT Token）及角色拥有的权限列表。前端据此控制页面展示与后续请求。

系统约定：**一个账号只属于一个角色**，不允许同一账号以多个角色身份登录。

#### 2.2.2 请求信息

- **URL**：`POST /sportsVenue/auth/login`
- **Method**：`POST`
- **Content-Type**：`application/json`

#### 2.2.3 请求参数

| 参数名      | 类型     | 是否必填 | 说明   |
| -------- | ------ | ---- | ---- |
| username | String | 是    | 用户名  |
| password | String | 是    | 密码   |
| role     | String | 是    | 登录身份（USER / OWNER / ADMIN） |

#### 2.2.4 请求示例

```json
{
  "username": "zhangsan",
  "password": "123456",
  "role": "USER"
}
```

#### 2.2.5 成功响应示例

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "zhangsan",
    "realName": "张三",
    "role": "USER",
    "token": "eyJhbGciOiJIUzI1NiJ9.xxx.yyy",
    "permissions": [
      "USER_VIEW_VENUE",
      "USER_VIEW_VENUE_DETAIL",
      "USER_BOOK_VENUE"
    ]
  }
}
```

#### 2.2.6 返回字段说明（data）

| 字段名        | 类型            | 说明                 |
| ----------- | ------------- | ------------------ |
| userId      | Long          | 用户主键 ID           |
| username    | String        | 用户名                |
| realName    | String        | 真实姓名               |
| role        | String        | 角色：USER / OWNER / ADMIN |
| token       | String        | 登录成功后生成的 JWT Token |
| permissions | Array<String> | 当前角色拥有的权限编码列表     |

> `permissions` 为字符串数组，前端据此控制菜单、按钮等展示，具体编码含义可在后端配置或文档中维护。

#### 2.2.7 常见失败响应

- 用户名不存在：

```json
{
  "code": 401,
  "message": "用户名不存在",
  "data": null
}
```

- 密码错误：

```json
{
  "code": 401,
  "message": "密码错误",
  "data": null
}
```

- 登录身份不匹配：

```json
{
  "code": 403,
  "message": "登录身份不匹配",
  "data": null
}
```

- 账号被禁用：

```json
{
  "code": 403,
  "message": "账号已被禁用",
  "data": null
}
```

---

后续如需扩展其他模块（如场馆预约、订单查询、仓库物资管理等）接口，可继续在本文件中以类似格式追加章节。

