<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tly.mapper.NoticeMapper">

    <resultMap id="NoticeResultMap" type="com.tly.entity.Notice">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="publishTime" column="publish_time"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createByUsername" column="create_by_username"/>
        <result property="updateByUsername" column="update_by_username"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="NoticeSelectColumns">
        n.id,
        n.title,
        n.content,
        n.status,
        n.publish_time,
        n.is_deleted,
        cu.username AS create_by_username,
        uu.username AS update_by_username,
        n.create_time,
        n.update_time
    </sql>

    <sql id="NoticeFromJoin">
        FROM notice n
        LEFT JOIN sys_user cu ON cu.id = n.create_by
        LEFT JOIN sys_user uu ON uu.id = n.update_by
    </sql>

    <insert id="insert" parameterType="com.tly.entity.Notice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice
        (title, content, status, publish_time, is_deleted, create_by, update_by, create_time, update_time)
        VALUES
        (#{title}, #{content}, #{status}, #{publishTime}, 0, #{createBy}, #{updateBy}, NOW(), NOW())
    </insert>

    <select id="selectById" parameterType="long" resultMap="NoticeResultMap">
        SELECT
        <include refid="NoticeSelectColumns"/>
        <include refid="NoticeFromJoin"/>
        WHERE n.id = #{id}
        LIMIT 1
    </select>

    <update id="update" parameterType="com.tly.entity.Notice">
        UPDATE notice
        SET
            title = #{title},
            content = #{content},
            status = #{status},
            publish_time = #{publishTime},
            update_by = #{updateBy},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="logicalDelete">
        UPDATE notice
        SET
            is_deleted = 1,
            update_by = #{updateBy},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="updateStatus">
        UPDATE notice
        SET
            status = #{status},
            publish_time = #{publishTime},
            update_by = #{updateBy},
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <select id="countManage" resultType="long">
        SELECT COUNT(1)
        FROM notice
        <where>
            is_deleted = 0
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="listManage" resultMap="NoticeResultMap">
        SELECT
        <include refid="NoticeSelectColumns"/>
        <include refid="NoticeFromJoin"/>
        <where>
            n.is_deleted = 0
            <if test="keyword != null and keyword != ''">
                AND n.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND n.status = #{status}
            </if>
        </where>
        ORDER BY n.create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countPublished" resultType="long">
        SELECT COUNT(1)
        FROM notice
        <where>
            is_deleted = 0
            AND status = 'PUBLISHED'
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="listPublished" resultMap="NoticeResultMap">
        SELECT
        <include refid="NoticeSelectColumns"/>
        <include refid="NoticeFromJoin"/>
        <where>
            n.is_deleted = 0
            AND n.status = 'PUBLISHED'
            <if test="keyword != null and keyword != ''">
                AND n.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY n.publish_time DESC, n.create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectPublishedById" parameterType="long" resultMap="NoticeResultMap">
        SELECT
        <include refid="NoticeSelectColumns"/>
        <include refid="NoticeFromJoin"/>
        WHERE n.id = #{id}
          AND n.is_deleted = 0
          AND n.status = 'PUBLISHED'
        LIMIT 1
    </select>

</mapper>

